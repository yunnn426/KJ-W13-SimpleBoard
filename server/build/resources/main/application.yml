server:
  port: 8090

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/boardTest
    username: boardTest
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
#      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
